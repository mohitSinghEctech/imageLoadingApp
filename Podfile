# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageLoadingApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageLoadingApp

pod 'SDWebImage', '~> 5.0'

  target 'ImageLoadingAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImageLoadingAppUITests' do
    # Pods for testing
  end

end
